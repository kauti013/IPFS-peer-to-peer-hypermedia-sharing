1) run make command inside the src folder
2) java ACN <port_number>
3) Enter valid input commands such as Peer, Publish, Unpublish, show_peers, show_metadata, show_published
4) Every instance of ACN can be run on different ports.